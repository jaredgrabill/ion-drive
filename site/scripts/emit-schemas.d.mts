/** Type surface of emit-schemas.mjs (kept by hand — the impl is plain JS + JSDoc). */
export declare function emitSchemas(srcDir: string, outDir: string): { files: string[] };
export declare function defaultSchemasDir(): string;
